#include<math.h>
#include <stdio.h>
void main()
{
    int sum=0;i,j,n,
    printf("Enter the value of n and x\n");
    scanf("%d%d",&n,&x);
    for(i=1;i<=n;i++)
    {
        
            if(i%2==1)
            {
                j=j*(i+3);
                sum=sum+(pow(x,(i+3))/j);
            }
            else if(i==2;)
        }
    }
}